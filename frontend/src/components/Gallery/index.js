import "./Gallery.css"

function Gallery() {


    return (
        <div>
            {/* {pics.map((pic) => (
                <img src={pic.download_url} alt="something" key={pic.id} className="image"/>
            ))} */}
        </div>
    );
}

export default Gallery;
